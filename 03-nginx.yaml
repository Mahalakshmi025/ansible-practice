- name: nginx install and run
  hosts: web
  become: yes #take sudo access
  tasks:
  - name: install nginx
    ansible.builtin.package: #hetrogeneous module that can for all ditsros
      name: nginx
      state: present
 